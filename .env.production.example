# Modelo para produção. NUNCA commite valores reais — este arquivo é só formato.
# Em produção, variável ausente derruba o boot (ver src/lib/config.ts).

# URL pública da API
NEXT_PUBLIC_API_URL=

# Postgres gerenciado (Railway)
DATABASE_URL=

# Redis gerenciado
REDIS_URL=
